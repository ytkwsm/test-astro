---
import config from '../../settings/config.json';

const startYear = config.site.startYear;
const authorName = config.author.name;
---
<footer class="SiteFooter">
  <small>©️{startYear} {authorName}</small>
</footer>